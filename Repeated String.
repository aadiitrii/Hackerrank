def repeatedString(s, n):
    count_a = s.count('a')
    full_repeats = n//len(s)
    remainder = n%len(s)
    a_in_remainder = s[:remainder].count('a')
    return count_a * full_repeats + a_in_remainder
